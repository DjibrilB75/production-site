'use client'

// 3D Background Scene - DISABLED for performance
// The portfolio section now uses a clean petrol shiny background
// with CSS-based effects instead of Three.js

export default function BackgroundScene() {
  // Return null to disable 3D rendering
  // This improves performance significantly
  return null
}
